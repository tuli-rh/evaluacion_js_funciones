# evaluacion_js_funciones

# 1. Módulo de Tesorería: Pago de Permisos (Cálculos y Retorno)
En este ejercicio tenemos 2 funciones (function): 

1-. montoPermiso(monto) :
    Esta función es donde "se procesan los datos" (valor de input), en esta función tenemos una condicional (if- else) en la que aplicamos operaciones aritmeticas para aplicar descuentos al valor ingresado por el usuario y finalmente retornar un resultado a la función principal pagoPermisos().

2-. pagoPermisos() : 
    Esta es la función principal, en la que primero se reciben los valores del input ubicado en el archivo index, y tambien retorna un resultado, esta funcion esta conectada con la función montoPermiso(monto).

# 2. Módulo de Salud: Fila de Vacunación (Arreglos y Prioridad)
En este ejercicio tenemos 4 funciones (function) y 2 botones: 

1-. filaNormal(nombre):

2-.filaEsperaNormal():

3-.filaUrgencia(nombre):

4-.filaEsperaUrgencia():

# 3. Módulo de Subsidios: Buscador de Beneficiarios (Ciclos e If)
En este ejercicio tenemos 2 funciones (function): 

1-. buscarNombre(nombre):
    Esta función es donde "se procesan los datos" (valor de input), en esta función tenemos un bucle for en el que recorremos la lista "beneficiarios" para saber si el nombre ingresado por el usuario pertenece a los beneficiarios, si pertenece se retornara "Beneficiario Verificado", sino se retornara "No registrado", estos resultados se retornaran a la función principal entregaBeneficios().

2-. entregaBeneficios(): 
    Esta es la función principal, en la que primero se reciben los valores del input ubicado en el archivo index, y tambien retorna un resultado, esta funcion esta conectada con la función buscarNombre(nombre).